import crypto from 'node:crypto';

export class ModerationService{
  constructor({repoFactory,now=()=>new Date()}={}){if(typeof repoFactory!=='function')throw new Error('repoFactory required');this.repoFactory=repoFactory;this.now=now;}
  async review(ctx,{productId,categorySchema={}}={}){const repo=this.repoFactory(ctx);const p=await repo.get('Product',productId);if(!p)throw new Error('product not found');const issues=[];if(categorySchema.forbidden)issues.push({code:'category_forbidden',severity:'block'});for(const f of categorySchema.mandatoryFields||[])if(p[f]==null&&p.attributes?.[f]==null)issues.push({code:'missing_field',field:f,severity:'block'});for(const d of categorySchema.documents||[])if(!(p.documents||[]).some(x=>x.type===d))issues.push({code:'missing_document',document:d,severity:'block'});for(const c of categorySchema.certificates||[])if(!(p.certificates||[]).some(x=>x.type===c))issues.push({code:'missing_certificate',certificate:c,severity:'block'});if(categorySchema.age&&Number(p.ageRestriction||0)<Number(categorySchema.age))issues.push({code:'age_restriction_missing',severity:'block'});const rec={id:`mod_${crypto.randomUUID()}`,productId,status:issues.some(x=>x.severity==='block')?'blocked':'approved',issues,reviewedAt:this.now().toISOString()};await repo.put('ModerationCase',rec);return rec;}
}
