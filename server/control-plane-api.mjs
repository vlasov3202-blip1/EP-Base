import {authenticateRequest,getPlatformRuntimeForTests} from './http-api.mjs';
import {ObservabilityService} from './observability.mjs';
import {DisasterRecoveryService} from './disaster-recovery.mjs';
import {CapabilityAccessService} from './security-hardening.mjs';

function json(res,status,payload){res.writeHead(status,{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'});res.end(JSON.stringify(payload));}
function requireAdmin(ctx){if(ctx.role!=='admin')throw Object.assign(new Error('platform admin required'),{status:403,code:'FORBIDDEN'});}
async function body(req,{maxBytes=250000}={}){let size=0;const chunks=[];for await(const c of req){size+=c.length;if(size>maxBytes)throw Object.assign(new Error('payload too large'),{status:413});chunks.push(c)}return JSON.parse(Buffer.concat(chunks).toString('utf8')||'{}')}

async function collect(store){
  const companyIds=await store.listCompanyIds();const out={companies:[],market:{products:0,offers:0,orders:0,unservedDemand:0},ai:{decisions:0,executed:0,requiresApproval:0,costUnits:0},autonomy:{executedActions:0,humanDecisions:0,score:100},integrations:{connections:0,errors:0},moderation:{total:0,open:0,blocked:0,aiWaiting:0,humanExceptions:0,appealsOpen:0,quarantined:0,incidentsOpen:0,recentExceptions:[],recentAppeals:[],recentIncidents:[],evidenceFiles:0,republications:0},experiments:{tests:0,running:0},featureFlags:{total:0,enabled:0},audit:{events:0},categories:new Map()};
  for(const companyId of companyIds){const dump=await store.exportCompany(companyId);const rec=dump.records||[];const e=n=>rec.filter(x=>x.__entity===n);const products=e('Product'),offers=e('Offer'),orders=e('Order'),demand=e('UnservedDemand'),decisions=e('Decision'),costs=e('AiCost'),connections=e('ChannelConnection'),connectorHealth=e('ConnectorHealth'),mods=e('ModerationCase'),appeals=e('ModerationAppeal'),humanExceptions=e('ModerationHumanException'),incidents=e('ModerationIncident'),evidence=e('ModerationEvidence'),republications=e('ModerationRepublication'),tests=e('MarketingTest'),flags=e('FeatureFlag');
    for(const p of products){const c=p.categoryId||p.category||'uncategorized';out.categories.set(c,(out.categories.get(c)||0)+1)}
    out.market.products+=products.length;out.market.offers+=offers.length;out.market.orders+=orders.length;out.market.unservedDemand+=demand.filter(x=>x.status==='open').length;
    out.ai.decisions+=decisions.length;out.ai.executed+=decisions.filter(x=>x.status==='executed').length;out.ai.requiresApproval+=decisions.filter(x=>x.status==='requires_approval').length;out.ai.costUnits+=costs.reduce((s,x)=>s+Number(x.providerCost||0),0);
    out.integrations.connections+=connections.filter(x=>x.enabled).length+connectorHealth.filter(x=>x.status==='healthy').length;out.integrations.errors+=connections.filter(x=>x.status==='error').length+connectorHealth.filter(x=>['error','degraded'].includes(x.status)).length;out.moderation.total+=mods.length;out.moderation.open+=mods.filter(x=>['AI_REVIEW','SECOND_AI_REVIEW','HUMAN_EXCEPTION'].includes(x.status)).length;out.moderation.blocked+=mods.filter(x=>['SELLER_ACTION_REQUIRED','AUTO_REJECTED','QUARANTINED','AI_REVIEW_REQUIRED','SECOND_AI_REVIEW','HUMAN_EXCEPTION'].includes(x.decision)).length;out.moderation.aiWaiting+=mods.filter(x=>['AI_REVIEW_REQUIRED','SECOND_AI_REVIEW'].includes(x.decision)).length;out.moderation.humanExceptions+=humanExceptions.filter(x=>x.status==='open').length;out.moderation.appealsOpen+=appeals.filter(x=>['submitted','ai_review','human_review'].includes(x.status)).length;out.moderation.quarantined+=mods.filter(x=>x.decision==='QUARANTINED').length;out.moderation.incidentsOpen+=incidents.filter(x=>x.status==='open').length;out.moderation.evidenceFiles+=evidence.filter(x=>x.status==='active').length;out.moderation.republications+=republications.filter(x=>x.status==='completed').length;out.moderation.recentIncidents.push(...incidents.filter(x=>x.status==='open').map(x=>({companyId,id:x.id,title:x.title,severity:x.severity,affectedCount:x.affectedCount,quarantinedCount:x.quarantinedCount,createdAt:x.createdAt})));out.moderation.recentExceptions.push(...humanExceptions.filter(x=>x.status==='open').map(x=>({companyId,id:x.id,moderationCaseId:x.moderationCaseId,priority:x.priority,reasonCodes:x.reasonCodes,createdAt:x.createdAt})));out.moderation.recentAppeals.push(...appeals.filter(x=>['submitted','ai_review','human_review'].includes(x.status)).map(x=>({companyId,id:x.id,moderationCaseId:x.moderationCaseId,status:x.status,previousDecision:x.previousDecision,createdAt:x.createdAt})));out.experiments.tests+=tests.length;out.experiments.running+=tests.filter(x=>x.status==='running').length;out.featureFlags.total+=flags.length;out.featureFlags.enabled+=flags.filter(x=>x.enabled).length;out.audit.events+=(dump.audit||[]).length;
    out.companies.push({companyId,users:(dump.users||[]).length,products:products.length,offers:offers.length,orders:orders.length,decisions:decisions.length,flags:flags.length,moderation:mods.length});
  }
  out.moderation.recentIncidents=out.moderation.recentIncidents.sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt))).slice(0,50);out.moderation.recentExceptions=out.moderation.recentExceptions.sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt))).slice(0,50);out.moderation.recentAppeals=out.moderation.recentAppeals.sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt))).slice(0,50);out.ai.costUnits=Number(out.ai.costUnits.toFixed(4));out.autonomy.executedActions=out.ai.executed;out.autonomy.humanDecisions=out.ai.requiresApproval;const denom=out.ai.executed+out.ai.requiresApproval;out.autonomy.score=denom?Math.round(out.ai.executed/denom*100):100;out.categories=[...out.categories.entries()].map(([id,count])=>({id,count})).sort((a,b)=>b.count-a.count);return out;
}

async function collectHealth(store,infra,ctx){
  const platform=await infra.health.check();const companyIds=await store.listCompanyIds();const companies=[];const totals={dlq:0,retries:0,connectorErrors:0,webhookFailures:0,aiErrors:0,decisionFailures:0,policyDenials:0,drReady:0,drDegraded:0,drUnknown:0};
  for(const companyId of companyIds){const cctx={...ctx,companyId,role:'admin'};const repoFactory=c=>store.tenant(c);const observability=await new ObservabilityService({repoFactory}).snapshot(cctx);const dr=await new DisasterRecoveryService({repoFactory,backupService:infra.backups||null}).health(cctx);totals.dlq+=observability.dlqSize;totals.retries+=observability.retries;totals.connectorErrors+=observability.connectorHealth.error;totals.webhookFailures+=observability.webhookFailures;totals.aiErrors+=observability.aiProvider.errors;totals.decisionFailures+=observability.decisionFailures;totals.policyDenials+=observability.policyDenials;if(dr.status==='ready')totals.drReady++;else if(dr.status==='degraded')totals.drDegraded++;else totals.drUnknown++;const critical=observability.serviceHealth.status==='critical'||observability.dlqSize>0;const degraded=critical||observability.connectorHealth.error>0||observability.aiProvider.errors>0||dr.status!=='ready';companies.push({companyId,status:critical?'critical':degraded?'degraded':'ok',observability,disasterRecovery:dr});}
  const platformStatus=platform?.status||platform?.overall||'unknown';const status=platformStatus==='down'||companies.some(x=>x.status==='critical')?'critical':platformStatus==='degraded'||companies.some(x=>x.status==='degraded')?'degraded':'ok';return{status,platform,totals,companies,generatedAt:new Date().toISOString()};
}

export async function handleControlPlaneApi(req,res){
  const url=new URL(req.url,'http://local');if(!url.pathname.startsWith('/api/v1/control-plane'))return false;
  try{
    const ctx=await authenticateRequest(req);requireAdmin(ctx);const rt=await getPlatformRuntimeForTests();const {store,infra}=rt;const access=new CapabilityAccessService({repoFactory:c=>store.tenant(c),roleCapabilities:{admin:['platform.*']}});
    if(req.method==='GET'&&url.pathname==='/api/v1/control-plane'){await access.require(ctx,'platform.control.read');const [base,health]=await Promise.all([collect(store),collectHealth(store,infra,ctx)]);return json(res,200,{...base,health,generatedAt:new Date().toISOString()});}
    const company=url.searchParams.get('companyId')||ctx.companyId;const cctx={...ctx,companyId,role:'admin'};const repo=store.tenant(cctx);
    if(req.method==='GET'&&url.pathname==='/api/v1/control-plane/health'){await access.require(ctx,'platform.health.read');return json(res,200,await collectHealth(store,infra,ctx));}
    if(req.method==='POST'&&url.pathname==='/api/v1/control-plane/health/verify-dr'){await access.require(ctx,'platform.health.verify');const service=new DisasterRecoveryService({repoFactory:c=>store.tenant(c),backupService:infra.backups||null});return json(res,200,{companyId:company,result:await service.verifyLatest(cctx)});}
    if(req.method==='GET'&&url.pathname==='/api/v1/control-plane/feature-flags'){await access.require(ctx,'platform.flags.read');return json(res,200,{items:await repo.list('FeatureFlag')});}
    if(req.method==='POST'&&url.pathname==='/api/v1/control-plane/feature-flags'){await access.require(ctx,'platform.flags.write');const p=await body(req);if(!p.id)throw new Error('id required');const rec={...p,id:String(p.id),enabled:Boolean(p.enabled),updatedAt:new Date().toISOString()};await repo.put('FeatureFlag',rec);return json(res,200,{item:rec});}
    const moderationIncidentResolve=url.pathname.match(/^\/api\/v1\/control-plane\/moderation\/incidents\/([^/]+)\/resolve$/);
    if(req.method==='POST'&&moderationIncidentResolve){await access.require(ctx,'platform.moderation.write');const payload=await body(req);return json(res,200,{incident:await rt.moderationIncidents.resolve(cctx,moderationIncidentResolve[1],payload)});}
    if(req.method==='GET'&&url.pathname==='/api/v1/control-plane/moderation'){await access.require(ctx,'platform.moderation.read');return json(res,200,{items:await repo.list('ModerationCase')});}
    if(req.method==='GET'&&url.pathname==='/api/v1/control-plane/experiments'){await access.require(ctx,'platform.experiments.read');return json(res,200,{tests:await repo.list('MarketingTest'),memory:await repo.list('MarketingMemory')});}
    if(req.method==='GET'&&url.pathname==='/api/v1/control-plane/audit'){await access.require(ctx,'platform.audit.read');return json(res,200,{items:(await store.exportCompany(company)).audit||[]});}
    return json(res,404,{error:'not found'});
  }catch(e){const status=e.code==='FORBIDDEN_CAPABILITY'?403:e.status||500;return json(res,status,{error:e.message,code:e.code||'CONTROL_PLANE_ERROR'});}
}
