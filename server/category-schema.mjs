import crypto from 'node:crypto';

export class CategorySchemaService{
  constructor({repoFactory,now=()=>new Date()}={}){if(typeof repoFactory!=='function')throw new Error('repoFactory required');this.repoFactory=repoFactory;this.now=now;}
  async put(ctx,input={}){if(!input.categoryId)throw new Error('categoryId required');const repo=this.repoFactory(ctx);const current=(await repo.list('CategorySchema')).filter(x=>x.categoryId===input.categoryId).sort((a,b)=>Number(b.version||0)-Number(a.version||0))[0];const rec={id:input.id||`catschema_${crypto.randomUUID()}`,categoryId:input.categoryId,version:Number(input.version||((current?.version||0)+1)),status:input.status||'active',attributes:structuredClone(input.attributes||{}),mandatoryFields:[...new Set(input.mandatoryFields||[])],compatibilityRules:structuredClone(input.compatibilityRules||[]),visualCapabilities:[...new Set(input.visualCapabilities||[])],moderation:structuredClone(input.moderation||{}),createdAt:this.now().toISOString()};await repo.put('CategorySchema',rec);return rec;}
  async active(ctx,categoryId){const rows=(await this.repoFactory(ctx).list('CategorySchema')).filter(x=>x.categoryId===categoryId&&x.status==='active').sort((a,b)=>Number(b.version||0)-Number(a.version||0));return rows[0]||null;}
  async validate(ctx,product={}){const schema=await this.active(ctx,product.categoryId||product.category);if(!schema)return{valid:true,missing:[],errors:[],schema:null};const missing=[];for(const f of schema.mandatoryFields||[]){if(product[f]==null&&product.attributes?.[f]==null)missing.push(f)}const errors=[];for(const [k,def] of Object.entries(schema.attributes||{})){const v=product.attributes?.[k];if(v==null)continue;if(def.type==='number'&&Number.isNaN(Number(v)))errors.push(`${k}:number`);if(def.enum&& !def.enum.includes(v))errors.push(`${k}:enum`)}return{valid:missing.length===0&&errors.length===0,missing,errors,schema};}
}
